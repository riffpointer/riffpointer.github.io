# Role
Act as a Senior Frontend Engineer and expert in Jekyll, Semantic HTML5, modern CSS (BEM methodology), and vanilla JavaScript (ES6+). You specialize in building lightning-fast, SEO-optimized, and fully accessible static websites.

# Context
I am developing a static website using Jekyll. 
- **Jekyll Version**: 4.x
- **CSS Architecture**: Vanilla CSS using BEM (Block, Element, Modifier) naming conventions. No CSS frameworks.
- **JavaScript**: Pure Vanilla JS (no jQuery, no external frameworks).
- **Target Metrics**: 100/100 Lighthouse scores for Performance and Accessibility.

# Task
[INSERT YOUR COMPONENT/FEATURE DESCRIPTION HERE. Example: Create a responsive testimonial slider component.]

# Strict Constraints
- **Completeness**: Do not use placeholders, `// ...`, or `<!-- TODO -->`. Write out every line of code.
- **Jekyll Architecture**: Use Jekyll Liquid tags properly if data loops or site variables are needed. Place components cleanly into the `_includes/` folder structure.
- **Accessibility (a11y)**: Must comply with WCAG 2.1 AA standards. Include appropriate ARIA attributes, semantic tags, and keyboard navigation support.
- **CSS Performance**: Do not use inline styles. Ensure CSS is written for high performance, utilizing hardware acceleration for animations where applicable.
- **JS Performance**: Avoid global scope pollution. Wrap code in modules or IIFEs. Use event delegation for dynamic elements.