High Priority
1. Add explicit JSON schema validation before loading or saving project records.
2. Add a dedicated "Save As" flow for exporting project data to a new file.
3. Add row-level dirty state so unsaved changes are visible in the table.
4. Add a confirmation step before overwriting an existing project file in preferences.
5. Add better error reporting when the JSON file is malformed or unreadable.
6. Add per-field validation feedback in the project editor dialog.
7. Add duplicate-ID highlighting in the table and editor before save.

Medium Priority
8. Add inline row filtering chips for name, tag, and demo status.
9. Add keyboard shortcuts for duplicate, delete, refresh, and search focus.
10. Add a project preview pane that shows the selected record in readable form.
11. Add import/export actions for JSON backup copies.
12. Add automatic backup creation before each save.
13. Add last-opened file persistence so the app restores the previous workspace.
14. Add a bulk tag editor for multiple selected projects.
15. Add a command palette for quick access to common actions.

Low Priority
16. Add a project statistics view with counts for tags, demos, and total records.
17. Add recent files history in the File menu.
18. Add per-column search in the header for faster scanning.
19. Add a compact card-style details drawer for the selected project.
20. Add export to CSV for spreadsheet workflows.
